# Auth0 Configuration for K6 Stress Testing
# Copy this file to .env and fill with your real credentials

AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_CLIENT_SECRET=your_client_secret_here
AUTH0_AUDIENCE=https://your-auth0-domain.auth0.com/api/v2/

# Gateway Configuration
GATEWAY_URL=http://localhost:9090
GATEWAY_TOKEN_ENDPOINT=http://localhost:9090/token/client-credentials

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your real Auth0 credentials in the .env file
# 3. The .env file is already in .gitignore and won't be committed
